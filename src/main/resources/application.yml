spring:
  application:
    name: taskmanagement

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:taskmanagement}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root@123}

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

logging:
  level:
    root: INFO

jwt:
  secret: 570c6e563d856a5199f04331b5b717d9fe3ab898596d20b4f1db1756a0c9b20d
  expiration: 3600000   # 1 hour in milliseconds

